services:
  - type: web
    name: bancovla-api-develop
    runtime: python
    plan: free
    region: oregon
    branch: develop
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn bancovla_api.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: "3.13.4"
      - key: ENVIRONMENT
        value: production
      - key: ALGORITHM
        value: HS256
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: "30"
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        sync: false
      - key: DJANGO_SECRET_KEY
        sync: false
      - key: ALLOWED_HOSTS
        sync: false
      - key: CORS_ALLOWED_ORIGINS
        sync: false
